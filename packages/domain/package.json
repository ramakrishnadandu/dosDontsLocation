{
  "name": "@locaguide/domain",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "jest"
  },
  "dependencies": {
    "@locaguide/config": "*",
    "@locaguide/contracts": "*",
    "@locaguide/providers": "*",
    "@locaguide/shared": "*"
  }
}
