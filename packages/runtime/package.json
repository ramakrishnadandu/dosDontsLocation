{
  "name": "@locaguide/runtime",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@locaguide/config": "*",
    "@locaguide/contracts": "*",
    "@locaguide/db": "*",
    "@locaguide/domain": "*",
    "@locaguide/providers": "*",
    "@locaguide/shared": "*"
  }
}
